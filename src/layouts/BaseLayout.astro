---
import { toolCategories, tools, type ToolCategoryId } from "@/data/tools";
import { workflowPackages, type WorkflowPackageId } from "@/data/workflowPackages";

type Props = {
  title: string;
  description: string;
  canonicalPath?: string;
  robots?: string;
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
};

const { title, description, canonicalPath = "/", robots, structuredData } = Astro.props;
function normalizePath(path: string) {
  const withSlash = path.startsWith("/") ? path : `/${path}`;
  return withSlash === "/" ? "/" : `${withSlash.replace(/\/+$/, "")}/`;
}

const normalizedCanonicalPath = normalizePath(canonicalPath);
const currentTool = tools.find((tool) => normalizePath(tool.path) === normalizedCanonicalPath);
const currentWorkflowPackage = workflowPackages.find(
  (workflowPackage) => normalizePath(workflowPackage.path) === normalizedCanonicalPath
);
const workflowCategoryMap: Record<WorkflowPackageId, ToolCategoryId> = {
  "photo-scan-submission": "image",
  "business-document-submission": "business",
  "freelance-billing": "business"
};
const currentWorkflowCategory = currentWorkflowPackage
  ? workflowCategoryMap[currentWorkflowPackage.id]
  : undefined;
const primaryWorkflowPackage = workflowPackages[0];
function isActivePath(path: string) {
  return normalizePath(path) === normalizedCanonicalPath;
}
function isActiveCategory(categoryId: ToolCategoryId) {
  const shouldHighlightWorkflowCategory = currentWorkflowPackage?.id !== primaryWorkflowPackage.id;
  return (
    currentTool?.category === categoryId ||
    (shouldHighlightWorkflowCategory && currentWorkflowCategory === categoryId)
  );
}
const canonical = new URL(
  normalizedCanonicalPath,
  Astro.site ?? "https://k-document-tool.pages.dev"
).toString();
const structuredDataItems = Array.isArray(structuredData)
  ? structuredData
  : structuredData
    ? [structuredData]
    : [];
const jsonLdItems = structuredDataItems.map((item) =>
  JSON.stringify(item).replaceAll("<", "\\u003c")
);
const analyticsPage = {
  path: normalizedCanonicalPath,
  title
};
---

<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {robots && <meta name="robots" content={robots} />}
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <link rel="icon" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600;700&family=IBM+Plex+Sans+KR:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <script is:inline>
      (() => {
        const localHosts = new Set(["localhost", "127.0.0.1", "::1"]);
        if (localHosts.has(window.location.hostname)) return;
        const beacon = document.createElement("script");
        beacon.defer = true;
        beacon.src = "https://static.cloudflareinsights.com/beacon.min.js";
        beacon.dataset.cfBeacon = '{"token":"54ee976818264e5bbc947006b289846f"}';
        document.head.appendChild(beacon);
      })();
    </script>
    {
      jsonLdItems.map((jsonLd) => (
        <script is:inline type="application/ld+json" set:html={jsonLd}></script>
      ))
    }
    <script is:inline define:vars={{ analyticsPage }}>
      (() => {
        const page = analyticsPage;

        function toolContext(node) {
          const root = node?.closest?.("[data-tool-id]");
          if (!root) return {};
          return {
            tool_id: root.dataset.toolId || null,
            tool_title: root.dataset.toolTitle || null
          };
        }

        function compact(value) {
          return String(value || "").replace(/\s+/g, " ").trim();
        }

        window.dataLayer = window.dataLayer || [];
        window.kdocTrack = function kdocTrack(eventName, params = {}) {
          const payload = {
            event: eventName,
            page_path: page.path,
            page_title: page.title,
            event_time: new Date().toISOString(),
            ...params
          };
          window.dataLayer.push(payload);
          window.dispatchEvent(new CustomEvent("kdoc:analytics", { detail: payload }));
          return payload;
        };

        function bootAnalytics() {
          const pageTool = document.querySelector("[data-tool-id]");
          window.kdocTrack("page_view", toolContext(pageTool));

          const startedTools = new Set();
          const markToolStart = (event) => {
            const target = event.target;
            if (!(target instanceof Element)) return;
            const root = target.closest("[data-tool-id]");
            if (!root) return;
            const toolId = root.dataset.toolId;
            if (!toolId || startedTools.has(toolId)) return;
            startedTools.add(toolId);
            window.kdocTrack("tool_start", toolContext(root));
          };

          document.addEventListener("input", markToolStart, true);
          document.addEventListener("change", markToolStart, true);
          document.addEventListener(
            "click",
            (event) => {
              const target =
                event.target instanceof Element
                  ? event.target.closest("[data-analytics-event]")
                  : null;
              if (!target) return;

              const params = {
                ...toolContext(target),
                label: target.dataset.analyticsLabel || compact(target.textContent),
                href: target instanceof HTMLAnchorElement ? target.href : undefined
              };

              if (target.dataset.analyticsToolId) params.tool_id = target.dataset.analyticsToolId;
              if (target.dataset.analyticsToolTitle) {
                params.tool_title = target.dataset.analyticsToolTitle;
              }
              if (target.dataset.analyticsShortcutId) {
                params.shortcut_id = target.dataset.analyticsShortcutId;
              }
              if (target.dataset.analyticsTargetPreset) {
                params.target_preset = target.dataset.analyticsTargetPreset;
              }
              if (target.dataset.analyticsPackageId) {
                params.package_id = target.dataset.analyticsPackageId;
              }
              if (target.dataset.analyticsProblemId) {
                params.problem_id = target.dataset.analyticsProblemId;
              }
              if (target.dataset.analyticsTargetToolId) {
                params.target_tool_id = target.dataset.analyticsTargetToolId;
              }
              if (target.dataset.analyticsTargetToolTitle) {
                params.target_tool_title = target.dataset.analyticsTargetToolTitle;
              }

              window.kdocTrack(target.dataset.analyticsEvent, params);
            },
            true
          );
        }

        if (document.readyState === "loading") {
          document.addEventListener("DOMContentLoaded", bootAnalytics, { once: true });
        } else {
          bootAnalytics();
        }
      })();
    </script>
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/" aria-label="K문서툴 홈">
        <span class="brand-mark">K</span>
        <span>문서툴</span>
      </a>
      <nav class="top-nav" aria-label="주요 도구">
        <a
          href="/tools/"
          class:list={{ "is-active": isActivePath("/tools/") }}
          aria-current={isActivePath("/tools/") ? "page" : undefined}
          data-analytics-event="catalog_nav_click"
          data-analytics-label="전체 도구"
        >
          전체 도구
        </a>
        <a
          href="/tools/submission-file-prep/"
          class:list={{ "is-active": isActivePath("/tools/submission-file-prep/") }}
          aria-current={isActivePath("/tools/submission-file-prep/") ? "page" : undefined}
          data-analytics-event="workflow_nav_click"
          data-analytics-label="제출용 파일 준비"
        >
          제출 준비
        </a>
        <a
          href={primaryWorkflowPackage.path}
          class:list={{ "is-active": isActivePath(primaryWorkflowPackage.path) }}
          aria-current={isActivePath(primaryWorkflowPackage.path) ? "page" : undefined}
          data-analytics-event="package_nav_click"
          data-analytics-package-id={primaryWorkflowPackage.id}
          data-analytics-label={primaryWorkflowPackage.title}
        >
          {primaryWorkflowPackage.shortTitle}
        </a>
        {
          toolCategories.map((category) => (
            <a
              href={category.path}
              class:list={{
                "is-active": isActivePath(category.path) || isActiveCategory(category.id)
              }}
              aria-current={
                isActivePath(category.path) || isActiveCategory(category.id) ? "page" : undefined
              }
              data-analytics-event="category_nav_click"
              data-analytics-label={category.shortTitle}
            >
              {category.shortTitle}
            </a>
          ))
        }
      </nav>
    </header>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    color-scheme: light;
    --bg: #f3f1eb;
    --surface: #fffefa;
    --surface-muted: #ece8df;
    --paper: #fffdf7;
    --ink: #161c1f;
    --muted: #596467;
    --line: #d6d0c4;
    --line-dark: #2e3637;
    --accent: #0b6f69;
    --accent-dark: #064d4a;
    --stamp: #b3312b;
    --danger: #a5302d;
    --success: #0b6f69;
    --shadow: 0 16px 34px rgba(38, 33, 25, 0.11);
    --radius: 4px;
    font-family:
      "IBM Plex Sans KR", "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", sans-serif;
  }

  * {
    box-sizing: border-box;
  }

  html {
    background: var(--bg);
    color: var(--ink);
  }

  body {
    margin: 0;
    min-width: 320px;
    background:
      radial-gradient(circle at 16px 16px, rgba(22, 28, 31, 0.04) 1px, transparent 1.5px) 0 0 / 24px 24px,
      linear-gradient(180deg, #fffefa 0, var(--bg) 430px),
      var(--bg);
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  button,
  input,
  select,
  textarea {
    font: inherit;
  }

  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    display: flex;
    min-width: 0;
    min-height: 68px;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    border-bottom: 2px solid var(--line-dark);
    background: rgba(255, 253, 247, 0.96);
    padding: 0 34px;
    backdrop-filter: blur(10px);
  }

  .brand {
    display: inline-flex;
    flex: 0 0 auto;
    min-height: 44px;
    align-items: center;
    gap: 10px;
    color: var(--ink);
    font-size: 21px;
    font-weight: 800;
    letter-spacing: 0;
  }

  .brand-mark {
    display: inline-grid;
    width: 30px;
    height: 30px;
    place-items: center;
    border: 2px solid var(--line-dark);
    border-radius: 4px;
    background: var(--accent);
    color: #fff;
    font-size: 18px;
    font-weight: 900;
  }

  .top-nav {
    display: flex;
    flex: 1 1 auto;
    max-width: 100%;
    min-width: 0;
    align-items: center;
    gap: 8px;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .top-nav::-webkit-scrollbar {
    display: none;
  }

  .top-nav a {
    display: inline-flex;
    flex: 0 0 auto;
    min-height: 44px;
    align-items: center;
    border-radius: 4px;
    color: var(--muted);
    font-size: 14px;
    font-weight: 700;
    padding: 10px 12px;
    white-space: nowrap;
  }

  .top-nav a:hover,
  .top-nav a:focus-visible,
  .top-nav a.is-active,
  .top-nav a[aria-current] {
    background: var(--surface-muted);
    color: var(--ink);
    outline: none;
  }

  .top-nav a.is-active,
  .top-nav a[aria-current] {
    background: #dfeeea;
    color: var(--accent-dark);
  }

  .page-shell {
    width: min(1440px, 100%);
    margin: 0 auto;
    padding: 34px;
  }

  .home-hero {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(460px, 1fr);
    min-height: calc(100vh - 104px);
    gap: 42px;
    align-items: center;
    border-bottom: 2px solid var(--line-dark);
    padding: clamp(30px, 6vw, 82px) 0 clamp(36px, 5vw, 68px);
  }

  .hero-copy {
    display: grid;
    gap: 24px;
    align-content: center;
  }

  .hero-copy h1 {
    max-width: 820px;
    margin: 0;
    color: var(--ink);
    font-size: clamp(40px, 6.2vw, 78px);
    font-weight: 800;
    letter-spacing: 0;
    line-height: 1.02;
    word-break: keep-all;
  }

  .hero-copy p {
    max-width: 660px;
    margin: 0;
    color: var(--muted);
    font-size: clamp(17px, 2vw, 21px);
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .hero-actions a {
    display: inline-flex;
    min-height: 48px;
    align-items: center;
    border: 2px solid var(--line-dark);
    border-radius: 4px;
    background: var(--line-dark);
    color: var(--paper);
    font-size: 15px;
    font-weight: 800;
    padding: 12px 16px;
  }

  .hero-actions a.secondary {
    background: transparent;
    color: var(--ink);
  }

  .hero-evidence {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    max-width: 640px;
  }

  .hero-evidence span {
    display: grid;
    gap: 3px;
    border-top: 2px solid var(--line-dark);
    padding-top: 9px;
    color: var(--muted);
    font-size: 13px;
    line-height: 1.45;
  }

  .hero-evidence strong {
    color: var(--ink);
    font-family: "IBM Plex Mono", monospace;
    font-size: 15px;
  }

  .workbench {
    position: relative;
    min-height: 520px;
    border: 2px solid var(--line-dark);
    background:
      linear-gradient(90deg, rgba(22, 28, 31, 0.05) 1px, transparent 1px) 0 0 / 28px 28px,
      linear-gradient(180deg, rgba(22, 28, 31, 0.05) 1px, transparent 1px) 0 0 / 28px 28px,
      #ebe4d8;
    padding: 24px;
    box-shadow: 10px 10px 0 rgba(22, 28, 31, 0.13);
  }

  .queue-sheet {
    position: relative;
    z-index: 2;
    display: grid;
    gap: 0;
    border: 2px solid var(--line-dark);
    background: var(--paper);
  }

  .queue-header,
  .queue-row {
    display: grid;
    grid-template-columns: 112px minmax(0, 1fr) 104px;
    gap: 14px;
    align-items: center;
    border-bottom: 1px solid var(--line);
    padding: 15px 16px;
  }

  .queue-header {
    background: var(--line-dark);
    color: var(--paper);
    font-family: "IBM Plex Mono", monospace;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.02em;
  }

  .queue-row:last-child {
    border-bottom: 0;
  }

  .queue-row span:first-child,
  .queue-row span:last-child {
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 12px;
    font-weight: 700;
  }

  .queue-row strong {
    display: block;
    color: var(--ink);
    font-size: 19px;
    line-height: 1.35;
  }

  .queue-row em {
    display: block;
    margin-top: 4px;
    color: var(--muted);
    font-style: normal;
    font-size: 13px;
    line-height: 1.55;
  }

  .paper-note {
    position: absolute;
    right: 28px;
    bottom: 28px;
    width: min(250px, 42%);
    border: 2px solid var(--line-dark);
    background: #fff6dc;
    padding: 16px;
    transform: rotate(-1.7deg);
    box-shadow: 5px 5px 0 rgba(22, 28, 31, 0.12);
  }

  .paper-note strong {
    display: block;
    color: var(--stamp);
    font-family: "IBM Plex Mono", monospace;
    font-size: 13px;
    margin-bottom: 8px;
  }

  .paper-note span {
    color: var(--ink);
    font-size: 15px;
    font-weight: 700;
    line-height: 1.55;
  }

  .desk-ruler {
    position: absolute;
    left: 18px;
    bottom: 42px;
    width: 44%;
    height: 32px;
    border: 2px solid var(--line-dark);
    background:
      repeating-linear-gradient(90deg, var(--line-dark) 0 1px, transparent 1px 22px),
      #d9c79e;
    transform: rotate(2deg);
  }

  .tool-intro {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 28px;
    align-items: end;
    margin-bottom: 24px;
  }

  .tool-intro h1 {
    margin: 0;
    color: var(--ink);
    font-size: clamp(34px, 5vw, 60px);
    line-height: 1;
    letter-spacing: 0;
  }

  .tool-intro p {
    max-width: 760px;
    margin: 14px 0 0;
    color: var(--muted);
    font-size: 18px;
    line-height: 1.65;
  }

  .quick-links {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    justify-content: flex-end;
  }

  .quick-links a {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--paper);
    color: var(--muted);
    font-size: 14px;
    font-weight: 800;
    padding: 10px 12px;
  }

  .workspace {
    display: grid;
    grid-template-columns: minmax(280px, 360px) minmax(0, 1fr) minmax(240px, 300px);
    gap: 18px;
    align-items: start;
  }

  .panel,
  .preview-panel,
  .side-panel {
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: var(--surface);
    box-shadow: var(--shadow);
  }

  .panel,
  .side-panel {
    padding: 18px;
  }

  .preview-panel {
    min-height: 620px;
    overflow: auto;
    padding: 22px;
  }

  .panel-title {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 16px;
  }

  .panel-title h2,
  .panel-title h3 {
    margin: 0;
    font-size: 16px;
    line-height: 1.2;
  }

  .field-grid {
    display: grid;
    gap: 12px;
  }

  .field {
    display: grid;
    gap: 6px;
  }

  .field span,
  .field label,
  .field legend {
    color: #344457;
    font-size: 13px;
    font-weight: 800;
  }

  input,
  select,
  textarea {
    width: 100%;
    min-height: 44px;
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--paper);
    color: var(--ink);
    font-size: 15px;
    line-height: 1.4;
    padding: 11px 12px;
  }

  textarea {
    min-height: 78px;
    resize: vertical;
  }

  input:focus,
  select:focus,
  textarea:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(17, 122, 122, 0.13);
    outline: none;
  }

  .button-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 16px;
  }

  .btn {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    gap: 8px;
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--paper);
    color: var(--ink);
    cursor: pointer;
    font-size: 14px;
    font-weight: 800;
    padding: 10px 12px;
  }

  .btn svg {
    width: 16px;
    height: 16px;
    stroke-width: 2;
  }

  .btn.primary {
    border-color: var(--accent);
    background: var(--accent);
    color: #fff;
  }

  .btn:hover {
    border-color: #aec0ce;
    background: var(--surface-muted);
  }

  .btn.primary:hover {
    border-color: var(--accent-dark);
    background: var(--accent-dark);
  }

  .doc-page {
    width: min(100%, 794px);
    min-height: 560px;
    margin: 0 auto;
    border: 1px solid #cad6df;
    background: var(--paper);
    color: #1a2531;
    box-shadow: 0 16px 38px rgba(20, 33, 47, 0.12);
  }

  .doc-inner {
    padding: clamp(20px, 4vw, 42px);
  }

  .doc-title {
    margin: 0 0 24px;
    text-align: center;
    font-size: clamp(26px, 4vw, 36px);
    line-height: 1.1;
    letter-spacing: 0;
  }

  .doc-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 14px;
  }

  .doc-table th,
  .doc-table td {
    border: 1px solid #8b9aaa;
    padding: 9px 10px;
    text-align: left;
    vertical-align: top;
  }

  .doc-table th {
    background: #f2f6f8;
    font-weight: 800;
  }

  .doc-table .total-korean-row th,
  .doc-table .total-korean-row td {
    background: #f8fbfc;
  }

  .doc-table .korean-total {
    color: var(--ink);
    font-weight: 900;
    word-break: keep-all;
    overflow-wrap: anywhere;
  }

  .doc-table .right {
    text-align: right;
  }

  .note-box {
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--surface-muted);
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
    padding: 12px;
  }

  .content-section {
    width: min(1120px, 100%);
    margin: 54px auto 0;
  }

  .content-section h2 {
    margin: 0 0 14px;
    font-size: 24px;
    letter-spacing: 0;
  }

  .content-section p,
  .content-section li {
    color: var(--muted);
    font-size: 16px;
    line-height: 1.75;
  }

  .section-heading {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 14px;
  }

  .section-heading h2 {
    margin: 0;
  }

  .section-heading p {
    max-width: 640px;
    margin: 0;
    color: var(--muted);
    font-size: 15px;
    line-height: 1.6;
  }

  .section-link {
    margin: 16px 0 0;
    text-align: right;
  }

  .section-link a {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    color: var(--accent-dark);
    font-weight: 900;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .tool-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0;
    border-top: 1px solid var(--line);
  }

  .tool-grid.single-card-grid {
    grid-template-columns: 1fr;
  }

  .tool-grid.compact-grid {
    grid-template-columns: 1fr;
  }

  .workflow-list {
    display: grid;
    gap: 10px;
  }

  .workflow-row {
    display: grid;
    grid-template-columns: 150px minmax(0, 1fr) minmax(240px, 340px);
    gap: 18px;
    align-items: center;
    border-top: 1px solid var(--line);
    padding: 18px 0;
  }

  .workflow-row:last-child {
    border-bottom: 1px solid var(--line);
  }

  .workflow-row:hover .workflow-row-copy strong,
  .workflow-row:focus-visible .workflow-row-copy strong {
    color: var(--accent-dark);
  }

  .workflow-row:focus-visible {
    border-radius: 4px;
    box-shadow: 0 0 0 3px rgba(11, 111, 105, 0.14);
    outline: none;
  }

  .workflow-row-kicker {
    color: var(--accent-dark);
    font-size: 13px;
    font-weight: 900;
  }

  .workflow-row-copy {
    display: grid;
    gap: 6px;
  }

  .workflow-row-copy strong {
    color: var(--ink);
    font-size: 22px;
    line-height: 1.25;
  }

  .workflow-row-copy span {
    color: var(--muted);
    font-size: 15px;
    line-height: 1.65;
  }

  .workflow-row-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    justify-content: flex-end;
  }

  .workflow-row-meta span,
  .featured-tool-query {
    border: 1px solid #cfe0dc;
    border-radius: 4px;
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 12px;
    font-weight: 900;
    line-height: 1.2;
    padding: 6px 8px;
  }

  .shortcut-list {
    display: grid;
    border-bottom: 1px solid var(--line);
  }

  .shortcut-row {
    display: grid;
    grid-template-columns: 128px minmax(0, 1fr) minmax(160px, 240px);
    gap: 16px;
    align-items: center;
    border-top: 1px solid var(--line);
    padding: 15px 0;
  }

  .shortcut-row:hover .shortcut-row-copy strong,
  .shortcut-row:focus-visible .shortcut-row-copy strong {
    color: var(--accent-dark);
  }

  .shortcut-row:focus-visible {
    border-radius: 4px;
    box-shadow: 0 0 0 3px rgba(11, 111, 105, 0.14);
    outline: none;
  }

  .shortcut-row-label {
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 13px;
    font-weight: 900;
  }

  .shortcut-row-copy {
    display: grid;
    gap: 5px;
  }

  .shortcut-row-copy strong {
    color: var(--ink);
    font-size: 18px;
    line-height: 1.35;
  }

  .shortcut-row-copy span {
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
  }

  .shortcut-row-tool {
    justify-self: end;
    border: 1px solid #cfe0dc;
    border-radius: 4px;
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 12px;
    font-weight: 900;
    line-height: 1.2;
    padding: 6px 8px;
  }

  .featured-tool-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    border-top: 1px solid var(--line);
  }

  .featured-tool-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 14px;
    align-items: start;
    border-bottom: 1px solid var(--line);
    padding: 16px 0;
  }

  .featured-tool-row:nth-child(odd) {
    padding-right: 18px;
  }

  .featured-tool-row:nth-child(even) {
    border-left: 1px solid var(--line);
    padding-left: 18px;
  }

  .featured-tool-row > span:first-child {
    display: grid;
    gap: 5px;
  }

  .featured-tool-row strong {
    color: var(--ink);
    font-size: 18px;
    line-height: 1.35;
  }

  .featured-tool-row span span {
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
  }

  .category-ledger {
    display: grid;
    border-top: 2px solid var(--line-dark);
    border-bottom: 2px solid var(--line-dark);
  }

  .category-row {
    display: grid;
    grid-template-columns: 118px minmax(0, 1fr) 68px;
    gap: 18px;
    align-items: center;
    border-bottom: 1px solid var(--line);
    padding: 18px 0;
  }

  .category-row:last-child {
    border-bottom: 0;
  }

  .category-row-status,
  .category-row-link {
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 12px;
    font-weight: 700;
  }

  .category-row-link {
    justify-self: end;
  }

  .category-row-copy {
    display: grid;
    gap: 5px;
  }

  .category-row-copy strong {
    color: var(--ink);
    font-size: 22px;
    line-height: 1.25;
  }

  .category-row-copy span {
    color: var(--muted);
    font-size: 15px;
    line-height: 1.65;
  }

  .category-row:hover .category-row-copy strong,
  .category-row:focus-visible .category-row-copy strong {
    color: var(--accent-dark);
  }

  .category-row:focus-visible {
    border-radius: 4px;
    box-shadow: 0 0 0 3px rgba(11, 111, 105, 0.14);
    outline: none;
  }

  .catalog-toolbar {
    display: grid;
    grid-template-columns: minmax(260px, 1fr) auto auto;
    gap: 12px;
    align-items: end;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: 16px 0;
  }

  .catalog-search-field {
    display: grid;
    gap: 6px;
  }

  .catalog-search-field span,
  .catalog-count {
    color: var(--muted);
    font-size: 13px;
    font-weight: 900;
  }

  .catalog-search-field input {
    max-width: 520px;
  }

  .catalog-filter-group {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    justify-content: flex-end;
  }

  .catalog-filter-group button {
    min-height: 44px;
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--paper);
    color: var(--muted);
    cursor: pointer;
    font-size: 14px;
    font-weight: 900;
    padding: 10px 12px;
  }

  .catalog-filter-group button:hover,
  .catalog-filter-group button:focus-visible,
  .catalog-filter-group button.is-active {
    border-color: #cfe0dc;
    background: #dfeeea;
    color: var(--accent-dark);
    outline: none;
  }

  .catalog-count {
    margin: 0;
    text-align: right;
    white-space: nowrap;
  }

  .catalog-tool-list {
    display: grid;
    border-bottom: 1px solid var(--line);
  }

  .catalog-tool-row {
    display: grid;
    grid-template-columns: 100px minmax(0, 1fr) minmax(260px, 420px);
    gap: 16px;
    align-items: center;
    border-top: 1px solid var(--line);
    padding: 16px 0;
  }

  .catalog-tool-row[hidden] {
    display: none;
  }

  .catalog-tool-row:hover .catalog-tool-copy strong,
  .catalog-tool-row:focus-visible .catalog-tool-copy strong {
    color: var(--accent-dark);
  }

  .catalog-tool-row:focus-visible {
    border-radius: 4px;
    box-shadow: 0 0 0 3px rgba(11, 111, 105, 0.14);
    outline: none;
  }

  .catalog-tool-category {
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 13px;
    font-weight: 900;
  }

  .catalog-tool-copy {
    display: grid;
    gap: 5px;
  }

  .catalog-tool-copy strong {
    color: var(--ink);
    font-size: 18px;
    line-height: 1.35;
  }

  .catalog-tool-copy span {
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
  }

  .catalog-tool-queries {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    justify-content: flex-end;
  }

  .catalog-tool-queries span {
    border: 1px solid #d4e2df;
    border-radius: 4px;
    color: var(--accent-dark);
    font-family: "IBM Plex Mono", monospace;
    font-size: 12px;
    font-weight: 900;
    line-height: 1.2;
    padding: 6px 8px;
  }

  .catalog-empty {
    border-bottom: 1px solid var(--line);
    color: var(--muted);
    margin: 0;
    padding: 18px 0;
  }

  .catalog-empty[hidden] {
    display: none;
  }

  .tool-card {
    display: grid;
    grid-template-columns: minmax(132px, 0.28fr) minmax(0, 1fr);
    min-height: 0;
    align-content: start;
    gap: 8px 18px;
    border: 0;
    border-bottom: 1px solid var(--line);
    border-radius: 0;
    background: transparent;
    padding: 18px 0;
    box-shadow: none;
  }

  .tool-card.compact-card {
    min-height: 0;
    padding: 16px 0;
  }

  .tool-card.compact-card strong {
    font-size: 18px;
  }

  .tool-card .meta {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    grid-row: 1 / span 3;
    align-content: start;
  }

  .tool-card strong {
    color: var(--ink);
    font-size: 20px;
    line-height: 1.25;
  }

  .tool-card p {
    margin: 0;
  }

  .meta-tag {
    width: max-content;
    max-width: 100%;
    border-radius: 4px;
    background: #dfeeea;
    color: var(--accent-dark);
    font-size: 13px;
    font-weight: 900;
    padding: 5px 8px;
  }

  .meta-tag.muted {
    background: var(--surface-muted);
    color: var(--muted);
  }

  .trust-note {
    border-top: 1px solid var(--line);
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
    margin: 0;
    padding-top: 10px;
  }

  .workflow-step-list {
    display: grid;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .workflow-step-list li {
    margin: 0;
  }

  .workflow-step-list a {
    display: grid;
    grid-template-columns: 38px minmax(0, 1fr);
    gap: 12px;
    align-items: start;
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--paper);
    padding: 16px;
    box-shadow: var(--shadow);
  }

  .workflow-step-list strong,
  .workflow-step-list span {
    display: block;
  }

  .workflow-step-list strong {
    color: var(--ink);
    font-size: 18px;
    line-height: 1.35;
  }

  .workflow-step-list span span {
    margin-top: 3px;
    color: var(--muted);
    font-size: 15px;
    line-height: 1.6;
  }

  .step-number {
    display: inline-grid;
    width: 32px;
    height: 32px;
    place-items: center;
    border-radius: 4px;
    background: var(--accent);
    color: #fff;
    font-size: 15px;
    font-weight: 900;
  }

  .faq-list {
    display: grid;
    gap: 10px;
  }

  details {
    border: 1px solid var(--line);
    border-radius: 4px;
    background: var(--paper);
    padding: 14px 16px;
  }

  summary {
    cursor: pointer;
    color: var(--ink);
    font-weight: 800;
  }

  .status-list {
    display: grid;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .status-list li {
    display: flex;
    gap: 9px;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.5;
  }

  .status-dot {
    width: 8px;
    height: 8px;
    flex: 0 0 auto;
    border-radius: 99px;
    background: var(--accent);
    margin-top: 7px;
  }

  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
  }

  @media (max-width: 1100px) {
    .home-hero {
      grid-template-columns: 1fr;
      min-height: 0;
    }

    .workbench {
      min-height: 460px;
    }

    .tool-grid.compact-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .workflow-row {
      grid-template-columns: 130px minmax(0, 1fr);
    }

    .workflow-row-meta {
      grid-column: 2;
      justify-content: flex-start;
    }

    .shortcut-row {
      grid-template-columns: 120px minmax(0, 1fr);
    }

    .shortcut-row-tool {
      grid-column: 2;
      justify-self: start;
    }

    .catalog-toolbar {
      grid-template-columns: 1fr;
    }

    .catalog-filter-group,
    .catalog-count {
      justify-content: flex-start;
      text-align: left;
    }

    .catalog-tool-row {
      grid-template-columns: 86px minmax(0, 1fr);
    }

    .catalog-tool-queries {
      grid-column: 2;
      justify-content: flex-start;
    }

    .workspace {
      grid-template-columns: minmax(280px, 360px) minmax(0, 1fr);
    }

    .side-panel {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 760px) {
    .site-header {
      align-items: flex-start;
      flex-direction: column;
      gap: 12px;
      padding: 14px 18px;
      position: static;
    }

    .top-nav {
      width: 100%;
      padding-bottom: 2px;
    }

    .page-shell {
      padding: 22px 16px 34px;
    }

    .home-hero {
      gap: 28px;
      padding: 24px 0 38px;
    }

    .hero-copy {
      gap: 18px;
    }

    .hero-evidence {
      grid-template-columns: 1fr;
    }

    .workbench {
      min-height: 0;
      padding: 14px;
      box-shadow: 5px 5px 0 rgba(22, 28, 31, 0.13);
    }

    .queue-header,
    .queue-row {
      grid-template-columns: 44px minmax(0, 1fr);
      gap: 10px;
      padding: 13px 12px;
    }

    .queue-header span:last-child,
    .queue-row span:last-child {
      display: none;
    }

    .paper-note,
    .desk-ruler {
      position: static;
      width: 100%;
      margin-top: 14px;
      transform: none;
    }

    .tool-intro {
      grid-template-columns: 1fr;
    }

    .quick-links {
      justify-content: flex-start;
    }

    .section-heading {
      align-items: start;
      flex-direction: column;
    }

    .tool-grid {
      grid-template-columns: 1fr;
    }

    .tool-grid.compact-grid {
      grid-template-columns: 1fr;
    }

    .tool-card {
      grid-template-columns: 1fr;
      gap: 8px;
    }

    .tool-card .meta {
      grid-row: auto;
    }

    .workflow-row {
      grid-template-columns: 1fr;
      gap: 8px;
      padding: 16px 0;
    }

    .workflow-row-meta {
      grid-column: auto;
      justify-content: flex-start;
    }

    .shortcut-row {
      grid-template-columns: 1fr;
      gap: 7px;
    }

    .shortcut-row-tool {
      grid-column: auto;
    }

    .featured-tool-list {
      grid-template-columns: 1fr;
    }

    .featured-tool-row {
      grid-template-columns: 1fr;
    }

    .featured-tool-row:nth-child(odd),
    .featured-tool-row:nth-child(even) {
      border-left: 0;
      padding-left: 0;
      padding-right: 0;
    }

    .category-row {
      grid-template-columns: 1fr;
      gap: 7px;
    }

    .category-row-link {
      justify-self: start;
    }

    .catalog-tool-row {
      grid-template-columns: 1fr;
      gap: 8px;
    }

    .catalog-tool-queries {
      grid-column: auto;
    }

    .workspace {
      grid-template-columns: 1fr;
    }

    .preview-panel {
      min-height: 0;
      padding: 14px;
    }

    .doc-inner {
      padding: 18px;
    }

    .doc-table {
      font-size: 12px;
      min-width: 620px;
    }

    .doc-inner {
      overflow-x: auto;
    }

    .button-row {
      margin-top: 14px;
    }
  }

  @media print {
    .site-header,
    .tool-intro,
    .panel,
    .side-panel,
    .content-section,
    .button-row {
      display: none !important;
    }

    body {
      background: #fff;
    }

    .page-shell,
    .workspace,
    .preview-panel {
      display: block;
      margin: 0;
      padding: 0;
      width: 100%;
      border: 0;
      box-shadow: none;
    }

    .doc-page {
      width: 100%;
      min-height: auto;
      border: 0;
      box-shadow: none;
    }
  }
</style>
